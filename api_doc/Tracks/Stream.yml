info:
  name: Stream
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/tracks/:id/stream"
  params:
    - name: id
      value: "1"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
