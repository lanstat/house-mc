info:
  name: Download track
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/tracks/:id/download"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
