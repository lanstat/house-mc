info:
  name: Upload tracks
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/tracks"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/lanstat/Music/Elley Duhé - MIDDLE OF THE NIGHT (Official Video)-oSHzUD-uqKY.mp3
        contentType: audio/mpeg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
