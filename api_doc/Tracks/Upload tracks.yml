info:
  name: Upload tracks
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/tracks"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/lanstat/Downloads/01 - BENEE - Night Garden.mp3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
