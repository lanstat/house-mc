info:
  name: Update album
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{url}}/albums/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "tesst"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
