info:
  name: Update artist
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{url}}/artists/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "BENNE2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
