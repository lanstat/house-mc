info:
  name: Add track to playlist
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/me/playlists/:id/tracks"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "trackId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
