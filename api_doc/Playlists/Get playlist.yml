info:
  name: Get playlist
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/me/playlists/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
