info:
  name: Create playlist
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/me/playlists"
  body:
    type: json
    data: |-
      {
        "name": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
