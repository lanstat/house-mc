info:
  name: Update playlist
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/me/playlists/:id"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "assss"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
